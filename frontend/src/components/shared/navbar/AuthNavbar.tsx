import React from "react";
import Logo from "../logo/Logo";

const AuthNavbar = () => {
  return (
    <div className="absolute left-5 top-0 md:top-6 z-50">
      <Logo />
    </div>
  );
};

export default AuthNavbar;
